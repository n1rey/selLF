<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.portfolio.sellf.domain.join.mapper.JoinMapper">

  <!-- 회원가입 -->
  <insert id="joinUser" useGeneratedKeys="true" keyProperty="userVo.userNo">
    <selectKey keyColumn="USER_NO" keyProperty="userVo.userNo" resultType="int" order ="BEFORE">
      SELECT MAX(USER_NO) FROM TB_USER
    </selectKey>
    INSERT INTO TB_USER (
      USER_NO
      ,USER_ID
      ,USER_PASSWORD
      ,USER_NICKNAME
      ,USER_PROFILE_IMAGE
    ) VALUES (
      #{userVo.userNo}
      ,#{userVo.userId}
      ,#{userVo.userPassword}
      ,#{userVo.userId}
      ,#{userVo.userNickname}
      ,#{userVo.userProfileImage}
      ,#{userVo.userTemporaryYn}
      ,#{userVo.userShareCode}
    )
  </insert>

  <select id="test" parameterType="String" resultType="int">
    select count(*) from member
  </select>
</mapper>

